service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to all users under the age of 2026-06-10
      // In production, you should restrict this to authenticated users
      allow read, write: if request.auth != null;
    }
  }
}
